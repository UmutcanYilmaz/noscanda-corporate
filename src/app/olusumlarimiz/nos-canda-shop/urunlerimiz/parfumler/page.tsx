import type { Metadata } from 'next';
import { Structure } from './structure';

export const metadata: Metadata = {
  title: "Perfume Collection | Noscanda Group",
  description: "Nos Canda kişisel parfüm koleksiyonu — Golden Chapter ve Niche serileri. Yakında sizlerle.",
};

export default function ParfumlerPage() {
  return <Structure />;
}
