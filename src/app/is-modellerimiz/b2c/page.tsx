import type { Metadata } from 'next';
import { Structure } from './structure';

export const metadata: Metadata = {
  title: "B2C İş Modeli | Noscanda Group",
  description: "Nos Canda B2C (tüketiciye doğrudan satış) iş modeli — Online mağaza, franchise kiosk and perakende kanalları.",
};

export default function B2CPage() {
  return <Structure />;
}
