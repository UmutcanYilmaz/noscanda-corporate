import type { Metadata } from 'next';
import { Structure } from './structure';

export const metadata: Metadata = {
  title: "Kurumsal Alan Kokulandırma | Noscanda Group",
  description: "Otel, lobi, AVM and kurumsal ofisler için profesyonel ambient kokulandırma çözümleri.",
};

export default function KurumsalAlanPage() {
  return <Structure />;
}
